version: '2'
services:
  db:
    image: mysql:5.7
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=yes
      - TZ=US/Eastern
    volumes:
      - "db:/var/lib/mysql"
volumes:
  db:
    driver: local
